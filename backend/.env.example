# ==============================================================================
# Knovana Backend Environment Configuration (Example)
# ==============================================================================

# ------------------------------------------------------------------------------
# 1. System & Server Settings
# ------------------------------------------------------------------------------
# Environment mode: development | test | production
NODE_ENV=development

# Host and Port for Hono server to listen on
KNOVANA_HOST=127.0.0.1
KNOVANA_PORT=8000

# ------------------------------------------------------------------------------
# 2. Security & Auth Settings
# ------------------------------------------------------------------------------
# JWT Secret Key: MUST be changed in production to a secure random string!
# You can generate a secure secret key using: openssl rand -base64 32
KNOVANA_JWT_SECRET=default-jwt-secret-please-change-in-production

# JWT expiration time in days
KNOVANA_JWT_EXPIRE_DAYS=30

# CORS Origins: comma-separated list of allowed origins.
# For Chrome Extensions, use: chrome-extension://<EXTENSION_ID>
# Wildcards and port ranges are supported (e.g. chrome-extension://*, http://localhost:*, http://127.0.0.1:*)
KNOVANA_CORS_ORIGINS=chrome-extension://*,http://localhost:*,http://127.0.0.1:*

# ------------------------------------------------------------------------------
# 3. Storage Settings
# ------------------------------------------------------------------------------
# Path to SQLite database file
KNOVANA_DB_PATH=data/knovana.db

# Path to the Obsidian vault knowledge base root directory
KNOVANA_KB_ROOT=knowledge-base

# ------------------------------------------------------------------------------
# 4. LLM & Agent Settings (DeepSeek / Anthropic API Compatibility)
# ------------------------------------------------------------------------------
# DeepSeek API endpoint mapping (compatible with Anthropic SDK structure)
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic

# DeepSeek Authentication Token / API Key
ANTHROPIC_AUTH_TOKEN=<你的 DeepSeek API Key>

# Main Claude SDK model routing to DeepSeek models
ANTHROPIC_MODEL=deepseek-v4-flash
ANTHROPIC_DEFAULT_OPUS_MODEL=deepseek-v4-pro[1m]
ANTHROPIC_DEFAULT_SONNET_MODEL=deepseek-v4-pro[1m]
ANTHROPIC_DEFAULT_HAIKU_MODEL=deepseek-v4-flash

# Claude Code subagents configuration
CLAUDE_CODE_SUBAGENT_MODEL=deepseek-v4-flash
CLAUDE_CODE_EFFORT_LEVEL=max
